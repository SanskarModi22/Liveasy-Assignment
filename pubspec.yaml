name: liveasy_assignment
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.3 <3.0.0'

dependencies:
  dropdown_button2: ^2.1.2
  flutter:
    sdk: flutter
  google_fonts: ^4.0.4
  logger: ^1.3.0
  otp_text_field: ^1.1.3

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/
